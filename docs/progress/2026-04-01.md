---
tags:
  - type/progress
  - area/process
  - status/log
---

# 2026-04-01 Progress

## 关联文档
- [[index]]
- [[roadmap/current]]
- [[roadmap/content-expansion-1000]]
- [[features/youtube-platform-spec]]
- [[definition-of-done]]
- [[weekly/project-progress-summary]]

## 今日核心进展
- 内容库扩充从 673 条提升至 1000+，并在清洗后稳定在 1065 条（curated 39 + expanded 1026）。
- 扩库后完成一次定向质量清理：legacy canonical 缺失与高集中簇告警已收敛到 0 告警。
- 诊断与训练计划已接入更大的内容池：诊断默认使用 curated + expanded 直链视频，计划候选深度同步提升。
- 训练计划文案完成两段优化：中文口令化压缩 + 英文温和压缩，并新增中文场景前缀（发球/网前/脚步/关键分）。
- docs 知识图谱完成同步：content-expansion 路线图入口、boundaries 引用、YouTube 平台规范与 DoD 对齐已落地。

## 数据规模与分布
- 扩库峰值：1116（39 curated + 1077 expanded）。
- 清理后规模：1065（39 curated + 1026 expanded）。
- 平台分布（当前）：Bilibili 400，YouTube 665。

## 关键功能更新

### 1) 诊断推荐扩池
- `diagnoseProblem` 默认内容池改为 `contents + expandedContents`（仍保留直链过滤）。
- 诊断页推荐上限提升到 5 条，用于更稳的 plan 候选种子。

### 2) 训练计划候选增强
- 计划候选池阈值提升：
  - `MAX_PLAN_CANDIDATES`：7 -> 10
  - `MIN_PLAN_CANDIDATES`：5 -> 7
- 保留 seeded 内容优先，避免“为了去重而牺牲主题贴合度”。

### 3) 训练计划文案优化
- UI 展示层加入 `compactPrompt`：
  - 中文：更短、提示型、口令化。
  - 英文：温和压缩，保持可读性。
- 中文新增场景前缀：`关键分：`、`发球：`、`网前：`、`脚步：`。

## 文档同步
- 新建并接入：[[roadmap/content-expansion-1000]]。
- 新建：[[features/youtube-platform-spec]]。
- 更新：[[index]]、[[roadmap/requirements]]、[[definition-of-done]]。
- 同步根目录草案：`内容库扩充至1000条方案.md` 与 docs 路线图文档一致。

## 校验与验证
- 数据校验：`npm run -s validate:data` 通过（0 错误、0 告警）。
- 核心回归：
  - `src/__tests__/content-display.test.ts`
  - `src/__tests__/diagnosis-matching.test.ts`
  - `src/__tests__/app-smoke.test.tsx`
- 生产构建：`npm run -s build` 多次通过。

## 风险与后续
- `thumbnail.ts` 的 YouTube 缩略图策略仍是 `mqdefault`，尚未实现方案文档中的 `maxresdefault -> hqdefault` fallback 链路。
- 平台占比当前偏向 YouTube，若需严格贴合 600/400 目标，需要后续再做分布回调。
- `scripts/verify-creators-existence.ts` 在历史终端记录中有 exit code 1 轨迹；当前脚本已做显式退出处理，建议在 CI 或干净终端再跑一次留档。
