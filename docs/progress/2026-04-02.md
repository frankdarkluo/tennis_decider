---
tags:
  - type/progress
  - area/process
  - status/log
---

# 2026-04-02 Progress

## 关联文档
- [[index]]
- [[roadmap/current]]
- [[product/boundaries]]
- [[product/definition-of-done]]
- [[superpowers/plans/2026-04-02-diagnosis-plan-consistency-and-hit-rate]]

## 今日核心进展
- 诊断到训练计划的主线进一步收紧：`primaryNextStep` 现在稳定贯穿 plan summary、target、Day 1 focus / goal / success criteria，减少 diagnosis 与 plan 的文案漂移。
- `/plan` 页 summary headline 直接展示 diagnosis 的主动作，用户从 diagnosis 进入 plan 时能持续看到同一条下一步。
- 诊断命中率补强了 3 条高价值口语输入：
  - `发球老是扔不准球` -> `serve-toss-consistency`
  - `正手怎么抡都不走球` -> `forehand-no-power`
  - `接发一上来就被顶回去` -> `return-under-pressure`

### 当日额外改动（研究与本地工作流相关）
- 去掉研究流程里不需要的 Actionability 提示（`plan` 与 `diagnose` 页面均已移除渲染，但保留预览页与测试覆盖）。
- 修复多个页面的 React hydration mismatch：延后在组件初始渲染中读取 `localStorage`（改为 `useEffect` 后读取），并在 `Header` 中对已存在的本地评估读取做了 mount 后检查。
- 避免库页（`/library`）基于日期的随机种子导致服务器/客户端排序不一致——暂时使用确定性种子以修复水合误差（可替换为服务器下发的每日种子）。
- i18n 与评估文案更新：
  - 中英两语的评估提示 `assessment.resumeDraft` 被移除（不再显示“已恢复你刚才做到一半的评估进度”）。
  - 中文题目 `你对打或比赛时脑子里在想什么？` 更新为 `你练球或者比赛时脑海里在想什么？`。
  - 英文选项更新：`fine_c_forcing`（Can you force errors from your opponent?）的四个选项替换为：
    - Mostly waiting for their errors
    - Sometimes I hit a pressuring ball
    - I can force errors consistently through steady pressure
    - I create scoring opportunities through point construction
- 暂时注释掉 profile 中 `years` 滑杆题以简化当前的评估流（保留注释以便将来恢复）。

所有以上改动均已提交并推送到 `origin/main`，并在本地运行 `npm run build` 验证通过。

## 文档与仓库整理
- 新增执行计划文档：[[superpowers/plans/2026-04-02-diagnosis-plan-consistency-and-hit-rate]]。
- 清理本地文档噪音：`docs/.obsidian/` 与根目录 `.obsidian/` 已加入 `.gitignore`。
- 删除不需要的 `docs/Untitled.base`，并把已跟踪的 `docs/.obsidian/*` 从 Git 中移除。

## 校验与验证
- 核心回归：
  - `src/__tests__/diagnosis-matching.test.ts`
  - `src/__tests__/content-display.test.ts`
  - `src/__tests__/app-smoke.test.tsx`
- 生产构建：`npm run build` 通过。

## 产出提交
- `7f89eba` `feat: tighten diagnosis to plan flow`
- `cad1939` `chore: ignore local obsidian workspace files`
