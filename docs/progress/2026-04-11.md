---
aliases:
  - 2026-04-11
tags:
  - type/progress
  - area/plan
  - area/diagnose
  - area/content
  - status/log
---

# 2026-04-11 Progress

## Related docs
- [[index]]
- [[roadmap/current]]
- [[weekly/project-progress-summary]]
- [[progress/2026-04-10]]

## 今日核心进展
- 4 月 11 日不是继续加新功能的一天，而是按当天的 wrap-up 主线把 `app-development` 真正收口的一天。主线是先把 `PR7`、`PR9` 和 diagnose thumbnail follow-up 固化，再把旧的 product inconsistency 和 study residue 收掉，最后刷新 roadmap / README。
- 到今天收工时，代码侧已经形成 3 个新的 clean commits：
  - `0f740f5 feat: expand long-tail diagnosis coverage`
  - `7f8e488 fix: unify recommendation thumbnail fallbacks`
  - `2420a9b refactor: close hidden routes and model boundaries`
- 结合昨天已经落地的 `PR8` 和今天确认稳定的 `PR7`，这条分支现在的 assessment → diagnose → plan 主链已经是结构化、可验证、可 review 的状态，而不是继续依赖零散 patch。

## PR7. Plan Blueprint Engine V2
- `PR7` 在今天的意义，不是“又改了一版计划文案”，而是把 `/plan` 的内部生成逻辑真正切到 blueprint-first 的结构化路径上。入口仍然保留兼容 facade，但上游 assessment / diagnosis 的结构化输入终于会被稳定吃进训练计划，而不再被旧 template branching 压平。
- 这次 plan engine 的核心是：
  - 引入 `PlanIntent`
  - 把 blueprint / content linking / assemble 独立到 `plan-core`
  - 保留 `getPlanTemplate()` / `getPlanFromAssessment()` 作为兼容入口
- 用户可见层面，训练日卡片现在明确围绕：
  - goal
  - drill
  - load / volume
  - execution focus
  - success criteria
  - linked-content rationale
- 这让计划更像可执行练习合同，而不是“内容推荐列表换一个计划标题”。
- `PR7` 今天确认仍然稳定，并且已在 branch history 中：
  - `cb6e7fe feat: add blueprint-based plan engine`

## PR9. Long-Tail Skill Coverage Pack
- 今天真正落地并 cleanly landed 的第二条主线是 `PR9`。这一步把之前长期偏薄、容易被 generic forehand/backhand 逻辑吞掉的技能域补成 first-class coverage，而不是只多加几个 keyword。
- 这次优先补强的域包括：
  - volley / net play
  - half-volley
  - overhead
  - slice
  - doubles
  - tactics / point construction
  - pressure / match handling
  - on-the-run / movement-context problems
- 具体新增和强化的 lane 包括：
  - `volley-contact-instability`
  - `half-volley-late-contact`
  - `overhead-spacing`
  - `slice-depth-control`
  - `doubles-poach-hesitation`
  - `doubles-formation-confusion`
  - `passive-point-construction`
  - `safe-short-collapse`
  - `key-point-indecision`
  - `on-the-run-late-contact`
  - `recovery-delay`
- 重要的是，这次不是只有 diagnose 变细。recommendation 和 plan 也一起跟上了这些域：
  - doubles 不再轻易塌回 singles baseline 训练
  - pressure / tactics 不再只剩 technique-only 或“去看比赛视频”
  - volley / half-volley / overhead / on-the-run 会产出真正不同的训练 focus 和 success criteria
- `PR9` 今天已经不是“在工作树里待 review”的状态，而是完成验证并提交：
  - `0f740f5 feat: expand long-tail diagnosis coverage`

## Diagnose thumbnail reliability follow-up
- 今天还把 `/diagnose` 推荐视频卡片的缩略图可靠性问题彻底收掉了。问题一开始是 Standard 路径更容易出现 broken / missing thumbnail，但真正收口后发现不只是 `<img>` failover，还包括 curated item metadata 缺失和 placeholder 视觉质量差。
- 这次最终收口成两层：
  - 统一 diagnose / deep / library / plan-linked content 的 shared thumbnail path
  - 对截图里的真实缺 metadata 项 `content_cn_d_01` 补本地 thumbnail 资源，而不是继续依赖占位符
- 现在 fallback 不再泄露标题首字，也不再留下“半坏 UI”感的灰块。真正没有缩略图时会走统一的中性占位；有可恢复资源的卡片则直接显示真实 thumbnail。
- 这部分今天也已独立提交：
  - `7f8e488 fix: unify recommendation thumbnail fallbacks`

## Wrap-up cleanup
- 除了 `PR9` 和 thumbnail 之外，今天真正把审计里列出的 must-fix / should-fix 继续往下收了，没有把 branch 停留在“功能虽然有了，但结构仍然漂”的状态。
- 今天完成的 cleanup 包括：
  - 删除旧的 `PlatformVideoSearch` live-search 路径和对应 API / provider，中止半成品搜索行为
  - 把 diagnose result surface 从旧的 3-layer expand 收成单一 details 展开
  - 为 problem tag 建立轻量但真实的 typed contract，而不是继续全仓库裸 string
  - 把 diagnose mediation / intake extract / scenario routes 从 scenario reconstruction client 直接依赖里拆出来，形成 `localModel` boundary
  - 把 `/video-diagnose` 从“隐藏但仍然可直接访问”收口成 route 级和 API 级都明确关闭
- 这一批 cleanup 今天一起形成了第三个 commit：
  - `2420a9b refactor: close hidden routes and model boundaries`

## 今日验证结果
- 今天的 wrap-up 不是只靠 spot check：
  - `npm test` 通过：`55 files / 358 tests`
  - `npm run build` 通过
  - `git diff --check` 通过
- 中途还修掉了一个真实的 full-suite 问题：`deep-diagnose-orchestrator.test.tsx` 里沿用旧 tag `second-serve-net`，已经替换成当前 canonical tag `second-serve-reliability`。
- 仍然保留的 repo-level 限制没有变化：
  - `npm run lint` 仍然会被当前仓库的 Next ESLint 初始化交互卡住，所以它还不是稳定的无交互验证入口

## 当前状态
- `PR7` 已完成、验证通过并提交。
- `PR9` 已完成、验证通过并提交。
- diagnose thumbnail reliability follow-up 已完成、验证通过并提交。
- wrap-up 中和 product inconsistency、hidden route、boundary 解耦相关的 cleanup 也已完成并提交。
- 4 月 11 日剩下的工作只剩 docs / roadmap / README 收尾与 branch push，不再有未 landed 的代码主线。

## 下一步建议
- 先把今天的 docs refresh 作为单独 commit 收进去，再把 `app-development` push 到远端。
- 之后如果要继续下一条功能线，应该从今天这个已经收口的 branch state 出发，而不是再回到“混着脏 diff 继续堆”的模式。
